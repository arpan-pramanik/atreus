{
  "name": "atreus-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Concept Drift Streaming Adaptive Machine Learning Platform",
  "scripts": {
    "dev": "node scripts/dev.js",
    "start": "node scripts/dev.js",
    "stop": "node scripts/stop.js",
    "backend": "PYTHONPATH=. .venv/bin/python app.py 8000",
    "frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build"
  }
}
